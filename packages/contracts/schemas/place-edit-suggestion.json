{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://contracts.reelmap.app/schemas/place-edit-suggestion.json",
  "title": "PlaceEditSuggestion",
  "description": "A proposed correction to a place's business info (T-083): the response to POST /api/v1/places/{place}/suggestions, and one row of GET /api/v1/me/venues/suggestions. The submitter is deliberately absent — an operator judges the proposal, not the person who filed it.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "place_id", "status", "is_owner_submission", "changes", "note", "created_at", "reviewed_at"],
  "properties": {
    "id": { "type": "string" },
    "place_id": { "type": "string" },
    "place": {
      "type": "object",
      "description": "Present only where the row is listed across venues, so the screen can name the restaurant without a second request.",
      "additionalProperties": false,
      "required": ["id", "name", "slug"],
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "slug": { "type": "string" }
      }
    },
    "status": {
      "type": "string",
      "enum": ["pending", "approved", "rejected", "actioned"],
      "description": "A verified operator's own FIELD edit comes back `approved` — it applied on submit. Everything else is `pending` until a moderator decides, including an operator's own submission when it carries a note. `actioned` settles a note-only row: there was no patch to apply, and a human dealt with it by hand (T-112)."
    },
    "is_owner_submission": {
      "type": "boolean",
      "description": "The row was the venue operator's own edit rather than a proposal awaiting review."
    },
    "changes": {
      "type": "array",
      "description": "One entry per changed field, in the allow-list's fixed order — a list rather than an object because JSON key order is not something a client may rely on, and both surfaces render these in order.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["field", "from", "to"],
        "properties": {
          "field": {
            "type": "string",
            "enum": [
              "name",
              "address_line1",
              "address_line2",
              "city",
              "region",
              "postal_code",
              "country_code",
              "cuisine_primary",
              "price_range",
              "phone",
              "website",
              "opening_hours_json"
            ],
            "description": "Column name — the same vocabulary the submit endpoint accepts. The picture fields are absent by design: a suggestion may not propose an image URL."
          },
          "from": {
            "allOf": [{ "$ref": "#/definitions/suggestedValue" }],
            "description": "The value the submitter was looking at, captured at submit time. A reviewer compares it against the place as it is now to spot a proposal that has been overtaken."
          },
          "to": { "$ref": "#/definitions/suggestedValue" }
        }
      }
    },
    "note": {
      "type": ["string", "null"],
      "maxLength": 2000,
      "description": "Free text the submitter wrote for everything the field form cannot express — \"this place closed down\", \"the pin is on the wrong side of the street\" (T-112). A row may carry a note, a set of field changes, or both; a note-only row has an empty `changes`, so any surface rendering these must survive that."
    },
    "created_at": { "type": "string", "format": "date-time" },
    "reviewed_at": { "type": ["string", "null"], "format": "date-time" }
  },
  "definitions": {
    "suggestedValue": {
      "description": "One side of a proposed change. `array` is bounded to a list of STRINGS rather than left open (T-128): the only array-valued member of the `field` enum is `opening_hours_json`, which place.json pins as a flat string[]. An unbounded \"array\" here generates `unknown[]`, which is the same union-that-pins-nothing that hid the opening-hours bug on the other endpoint.",
      "type": ["string", "number", "array", "null"],
      "items": { "type": "string" }
    }
  }
}
