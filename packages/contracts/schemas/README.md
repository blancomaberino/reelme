# schemas/

Per-resource JSON Schemas (share, place, analysis-run, offer, redemption,
ledger-entry, errors) land here as the API surfaces them (01-architecture §6).

The **canonical AI extraction contract already lives one level up** at
[`../extraction.schema.json`](../extraction.schema.json) — it predates this
directory (T-005) and is referenced by path from the API (T-021) and the
ROADMAP exit criteria, so it stays where it is.
