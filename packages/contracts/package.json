{
  "name": "@reelmap/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared JSON Schemas + generated TS types — the single source of truth for Reelmap API/mobile data shapes.",
  "type": "commonjs",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "tsx scripts/generate.ts",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "jest": "^29.7.0",
    "json-schema-to-typescript": "^15.0.4",
    "ts-jest": "^29.2.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
