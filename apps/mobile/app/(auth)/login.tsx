import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function LoginScreen() {
  return <PlaceholderScreen title="Log in" subtitle="Email/password + social sign-in (T-010)" />;
}
