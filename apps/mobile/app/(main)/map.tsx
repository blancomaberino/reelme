import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function MapScreen() {
  return <PlaceholderScreen title="Map" subtitle="Discovery map with clustered pins (T-032)" />;
}
