import { PlaceholderScreen } from '@/components/placeholder-screen';

/** Search over GET /search — real UI lands in T-034. Reached from the map/feed headers. */
export default function SearchScreen() {
  return <PlaceholderScreen title="Search" subtitle="Places, tags and influencers (T-034)" />;
}
